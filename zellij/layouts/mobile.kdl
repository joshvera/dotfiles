// A minimalist layout that starts with a single, maximized pane.
// Ideal for small screens or a simple, focused workspace.
// Shows tab names for navigation awareness.

layout {
    pane size=1 borderless=true {
        plugin location="zellij:tab-bar"
    }
    pane
}